const first_number = window. prompt ("첫번째 숫자 입력")
const second_number = window.prompt ("두번째 숫자 입력")

const ab = Number(first_number) + Number(second_number)

window.alert (`${ab}`)

const age = window.prompt ("몇살입니까")

const ba = 2025 - Number(age) +1

window.alert (`귀하의 태어난 년도는 ${ba}년도 입니다`)